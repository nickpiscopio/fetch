object ServerEnvironment {
    // I would change this to a utility class ans switch this based on the build type (e.g. dev, staging, prod, etc)
    const val BASE_URL = "https://fetch-hiring.s3.amazonaws.com/"
}